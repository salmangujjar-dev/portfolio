export type TSocialIcons = {
  label: string;
  url: string;
};

export type TContactForm = {
  fullName: string;
  email: string;
  subject: string;
  body: string;
};

export type TProjects = {
  imageSrc: string;
  title: string;
  url: string;
  technologies: string[];
  description: string;
  features: string[];
};

export type TCareer = {
  role: String;
  company: String;
  companyLink: String;
  duration: String;
  details: String;
};
